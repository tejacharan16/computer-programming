def find_max_and_min(tpl):
    """
    Problem 7: Find the maximum and minimum elements in a tuple.
    Input: tpl = (10, 20, 5, 40)
    Output: (40, 5)
    Explanation: 40 is the maximum and 5 is the minimum element in the tuple.
    """
    return (max(tpl), min(tpl))
